docker run --rm \
  --name test-postgres \
  -e POSTGRES_USER=test \
  -e POSTGRES_PASSWORD=test \
  -e POSTGRES_DB=testdb \
  -p 5433:5432 \
  postgres:17

AUTH_DB="postgresql+psycopg://test:test@localhost:5433/testdb" \
alembic -c auth_service/alembic.ini upgrade head